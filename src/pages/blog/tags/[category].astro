---
import TagListPage from '@/components/blog/tag/TagListPage.astro'
import { TagCategoryEntries } from '@/content/config'

export function getStaticPaths() {
  return TagCategoryEntries.map(([category]) => ({
    params: { category },
    props: { category },
  }))
}

const { category } = Astro.props
---

<TagListPage category={category} />
